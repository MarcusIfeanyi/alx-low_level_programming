#include "main.h"
/**
 * print_numbers - Entry into the fxn
 *
 * Return: 0 for success
 */
void print_numbers(void)
{
	int num;

	for (num = 48; num <= 57; num++)
	{
		_putchar(num);
	}
	_putchar('\n);
}	
